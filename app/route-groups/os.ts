import { route } from "@react-router/dev/routes";

/**
 * "OS" surface — full-screen standalone screens that bring their own header and
 * bottom bar, so they are registered outside the sidebar layout.
 */
export default [route("os/accounts/main", "routes/os/accounts/main/index.tsx")];
